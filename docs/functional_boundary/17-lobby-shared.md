# DungeonChessBattle.Lobby.Shared

大厅共享值类型库，所属分组 Shared。存放跨服务端存储、协议传输与客户端展示共用的房间领域值类型，避免存储抽象依赖网络契约层。

## 职责

- 房间状态枚举 `RoomStatus`：等待、进行中、已结束，招募板与房间 DTO 共用。

## 边界外

- 不含业务实现与运行时逻辑，纯 .NET 类库，零项目引用。
- 不承载网络 DTO 与传输结构，网络契约仍归 Protocol。

## 依赖

- 无：纯 .NET 类库。
