# DungeonChessBattle.GameConfig

单位与副本配置库。纯 C# 配置数据库，零反射，编译期类型安全，服务端与客户端共用同一套配置。

## 职责

- Buff、技能、单位、副本等静态配置数据。
- 单位配置直接装配领域行为，无状态实例可多单位、多房间共享。
- 单位与副本的权威登记点，配置键与配置模型映射。
- 默认副本键与配置读取契约。

## 边界外

- 不含配置消费逻辑，敌人生成、结算、展示全由消费方实现。
- 不做运行时行为与反射，配置即数据。
- 不越过登记点，新增单位/副本必须经登记。

## 依赖

- Battle.Shared。
