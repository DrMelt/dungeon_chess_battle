# DungeonChessBattle.GameConfig

单位与副本配置库，所属分组 Shared。纯 C# 配置数据库，零反射，编译期类型安全，服务端与客户端共用同一套配置。

## 职责范围

- 静态配置数据：Buff、技能、单位、副本及其模型。
- 敌人智能实例以单位配置属性直接引用，无状态实例可多单位、多房间共享。
- 权威登记点：单位与副本登记，配置键与配置模型映射。
- 读取接口：配置读取契约，供 Godot 脚本访问。

## 不负责

- 不含配置消费逻辑，敌人生成、结算、展示全由消费方实现。
- 不做运行时行为与反射，配置即数据。
- 不越过登记点，新增单位/副本必须经登记。


## 依赖项

- Battle.Domain。
